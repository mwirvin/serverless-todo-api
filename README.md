# serverless-todo-api
A fully serverless Todo REST API built with AWS Lambda, API Gateway, and S3. Supports full CRUD operations (Create, Read, Update, Delete) using a Lambda function and an S3 JSON object as the datastore.
